# Instalar con:  pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0          # motor de Machine Learning (si falla, se usa scikit-learn)
openpyxl>=3.1          # lectura de .xlsx
XlsxWriter>=3.1        # escritura con formato
PyYAML>=6.0
scipy>=1.10

# Solo para correr los tests:
# pytest>=7.0
